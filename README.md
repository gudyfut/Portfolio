# Murilo Monferrari Portfolio

A personal portfolio website built with Next.js, TypeScript, Tailwind CSS, and the App Router.

## Getting Started

Install dependencies:

```bash
npm install
```

Run the development server:

```bash
npm run dev
```

Build for production:

```bash
npm run build
```

Open http://localhost:3000 to view the site.
